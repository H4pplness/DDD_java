Run keycloak in docker by command : 
// build 
docker build -t keycloak-membread .

// run 
docker run -d -p 8080:8080 keycloak-membread 

Then run project, add -data.sample=true flag to create model in database
